# Core (training + local eval)
torch>=2.1.0
transformers>=4.45.0
peft>=0.12.0
datasets>=2.16.0
accelerate>=0.33.0
safetensors>=0.4.0

# OpenAI-compatible clients (vLLM / SGLang / remote APIs)
openai>=1.40.0

# Vision / IO
Pillow>=10.0.0

# RAG index build + retrieval
numpy>=1.24.0
sentence-transformers>=3.0.0
pypdf>=4.0.0

# Metrics (evaluate_*.py)
nltk>=3.8.0
rouge-score>=0.1.2
bert-score>=0.3.13

# Optional: serve VLMs locally (install separately if needed; CUDA build varies by platform)
# vllm>=0.6.0
